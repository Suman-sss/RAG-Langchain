# Sample Raw Images

- `vpn_login_error.bmp`: simulated VPN login failure screen mentioning expired MFA token and VPN-401.
- `mfa_setup_failure.bmp`: simulated MFA setup screen after password reset.
- `account_locked_alert.bmp`: simulated account lock alert from a security dashboard.
- `laptop_provisioning_queue.bmp`: simulated operations board showing new-hire device queue states.

These are intentionally stored as raw image files so the intermediate project can implement OCR or other extraction logic directly from images.
